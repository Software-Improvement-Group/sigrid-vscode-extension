import { ActiveEditorChangedCommand } from './active-editor-changed-command';

describe('ActiveEditorChangedCommand', () => {
  it('should create an instance', () => {
    expect(new ActiveEditorChangedCommand()).toBeTruthy();
  });
});
