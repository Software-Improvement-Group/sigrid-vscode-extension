import { WebviewBaseUriCommand } from './webview-base-uri-command';

describe('WebviewBaseUrlCommand', () => {
  it('should create an instance', () => {
    expect(new WebviewBaseUriCommand()).toBeTruthy();
  });
});
