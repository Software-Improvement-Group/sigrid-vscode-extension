import { SigridFocusable } from './sigrid-focusable';

describe('SigridFocusable', () => {
  // todo
  // it('should create an instance', () => {
  //   expect(new SigridFocusable()).toBeTruthy();
  // });
});
