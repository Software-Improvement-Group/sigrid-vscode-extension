import { SecurityFindingMapper } from './security-finding-mapper';

describe('SecurityMapper', () => {
  it('should create an instance', () => {
    expect(new SecurityFindingMapper()).toBeTruthy();
  });
});
