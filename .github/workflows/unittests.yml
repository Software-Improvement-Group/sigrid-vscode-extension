name: unittests
on: [push]
jobs:
  unittests:
    name: "Unit tests"
    runs-on: ubuntu-latest
    steps:
      - name: "Check out repository"
        uses: actions/checkout@v4
      - run: "npm ci"
      - run: "npm audit --audit-level=critical"
      - run: "cd sigrid-ext-app && npm install && npm audit --audit-level=critical && npm test --coverage"
